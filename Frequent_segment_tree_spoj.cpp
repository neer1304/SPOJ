#include<cstdio>

int main()
{
    int n,q,i;

    scanf("%d",&n);
    while(n!=0)
    {
        scanf("%d",&q);
        int arr[n];

        for(i=0;i<n;i++)
            scanf("%d",&arr[i]);

        int st,en;

        while(q--)
        {
            scanf("%d %d",&st,&en);
        }

    }
}
